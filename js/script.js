window.addEventListener('DOMContentLoaded', () => {
  VANTA.BIRDS({
    // id
    el: "#vanta",
    mouseControls: true,
    touchControls: true,
    gyroControls: false,
    minHeight: 200.00,
    minWidth: 200.00,
    scale: 1.00,
    scaleMobile: 1.00,
    // agar menggunakan background dari kita
    backgroundAlpha: 0.0,
  })
})